name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Allow the workflow to publish to Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; don't cancel a run that's already publishing.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out the site
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # Turn Pages on via the API if it isn't already, rather than failing
          # and requiring someone to flip it in Settings first.
          enablement: true

      - name: Upload the site as an artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # The site is plain static files at the repository root.
          path: .

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
